version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DB_HOST=localhost
      - DB_USER=admin
      - DB_PASSWORD=L4nt3rn22
      - DB_NAME=backend
      - DB_PORT=5432
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - JWT_SECRET=4f76f84e786f50b7a8d9468fa0be69bd4bd03d0017013ecb5e1d4ac5bc816dcac6ab1d42e1300a5b5de5f44796304420914b479b1926bb68f3d284a1b2931539
      - JWT_EXPIRES_IN=1d
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
    command: npm run dev

  db:
    image: postgres:14
    environment:
      POSTGRES_DB: backend
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: L4nt3rn22
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:6
    ports:
      - '6379:6379'

volumes:
  postgres_data:
